<flynt-component 
  load:on="idle"
  {# client:media="(max-width: 1023px)" #}
  name="NavigationBurger"
  class="block lg:hidden fixed top-0 left-0 w-screen z-50 flex-row justify-between mx-auto {% if post.pageBackgroundCustom %}{{ post.pageBackgroundCustom }}{% else %}bg-white{% endif %}">
  <nav aria-label="{{ labels.ariaLabel|e }}" class="h-auto w-full">
    <div class="w-full h-auto max-w-screen-max mx-auto boxed grid grid-cols-2 lg:gap-sm align-middle items-center z-50 relative">
      {% include 'Partials/_logo.twig' with { 'logo': logo, 'site': site } only %}
      {% if menu %}
        <div class="flex justify-end items-center">
          {% include 'Partials/_hamburger.twig' with { 'labels': labels } only %}
        </div>
      {% endif %}
    </div>
    {% if menu %}
      <ul
        data-ref="menu" 
        class="menu w-full h-screen !pt-[50px] z-40 relative">
        <div class="w-full h-full max-w-screen-max mx-auto flex flex-col justify-start items-start gap-xs p-xs {% if post.pageBackgroundCustom %}{{ post.pageBackgroundCustom }}{% else %}bg-white{% endif %}">
          {% for item in menu.items %}
            {# regular menu item #}
            {% if not item.children %}
              <li class="item {{- item.current ? ' current-menu-item text-orange' }}">
                <a class="link font-menu" href="{{ item.link }}" {{ item.target == '_blank' ? 'target="_blank" rel="noopener noreferrer"' }}>
                  {{ item.title }}
                </a>
              </li>
            {% endif %}
            {# menu with submenu item #}
            {% if item.children %}
              <li class="item open-submenu {{- item.current ? ' current-menu-item text-orange' }}" >
                <a class="link font-menu" href="{{ item.link }}" {{ item.target == '_blank' ? 'target="_blank" rel="noopener noreferrer"' }}>
                  {{ item.title }}
                </a>
                <ul class="submenu">
                  {% for subitem in item.children %}
                    <li class="item {{- subitem.current ? ' current-menu-item text-orange' }}">
                      <a class="link !p-0 font-menu" href="{{ subitem.link }}" {{ subitem.target == '_blank' ? 'target="_blank" rel="noopener noreferrer"' }}>{{ subitem.title }}</a>
                    </li>
                  {% endfor %}
                </ul>
              </li>
            {% endif %}
          {% endfor %}
        <div>
      </ul>

    {% endif %}
  </nav>
</flynt-component>
