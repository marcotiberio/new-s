<img 
    class="figure-image lazyload object-cover"
    src="{{ item.image.src }}"
    alt="{{ item.image.alt|e | default('') }}"
    loading="lazy"
    style="opacity: 0; transition: opacity 0.5s ease-in-out;"
    onload="this.style.opacity='1'"
/>
