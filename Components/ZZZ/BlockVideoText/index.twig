<flynt-component 
  load:on="visible" 
  name="BlockVideoText" 
  style="
    color: {% if options.colorText %}{{ options.colorText }}{% else %}var(--black){% endif %};
    background-color: {% if options.colorBackground %}{{ options.colorBackground }}{% else %}transparent{% endif %};">
  <div class="wrapper max-w-screen-max mx-auto boxed">
    <div class="h-auto flex align-start justify-start md:justify-between" {{ videoAlignment ? 'data-video-alignment="' ~ videoAlignment ~ '"' }} {{ videoPosition ? 'data-video-position="' ~ videoPosition ~ '"' }}>
      <div class="w-full lg:w-1/2 h-full flex flex-col">
        {% if oembed %}
        <div class="video w-full">
          {# {% include 'Partials/_figure.twig' ignore missing with {image: posterImage} only %} #}
          <div data-ref="videoPlayer" class="video-player w-full mx-auto" data-state="isLoaded">
            {{ oembed }}
            <div class="video-loader"></div>
            <button data-ref="playButton" class="video-playButton" type="button" aria-label="Play Video"></button>
          </div>
        </div>
        {% endif %}
      </div>
      <div class="w-full lg:w-1/2 h-full flex flex-col justify-between">
        {% if contentHtml %}
          <div class="wysiwyg">{{ contentHtml|e('wp_kses_post') }}</div>
        {% endif %}
        {# {% if buttonLink %}
          <a class="button button--grey text-button" href="{{buttonLink.url}}">{{buttonLink.title}}</a>
        {% endif %}
        {% if seeAllButton %}
          <div class="w-full p-sm md:p-md flex flex-row items-center justify-center">
            <a class="button button--fullBlue" href="{{ seeAllButton.url }}"{% if seeAllButton.target %} target="{{ seeAllButton.target }}" rel="noreferrer noopener"{% endif %}>
              {{ seeAllButton.title|e }}
            </a>
          </div>
        {% endif %} #}
      </div>
    </div>
  </div>
</flynt-component>
