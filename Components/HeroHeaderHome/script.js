export default (heroHeaderHome) => {
  // Pure CSS marquee - no JavaScript needed for animation
  // Animation is handled by CSS keyframes in _style.scss
  
  return () => {
    // Cleanup function (currently no cleanup needed)
  }
}